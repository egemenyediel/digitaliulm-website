services:
  # Backend API Service
  - type: web
    name: digitaliulm-api
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
